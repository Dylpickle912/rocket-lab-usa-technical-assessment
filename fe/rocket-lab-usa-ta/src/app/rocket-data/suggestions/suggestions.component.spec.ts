import { SuggestionsComponent } from './suggestions.component';

describe('SuggestionsComponent', () => {
  let component: SuggestionsComponent;

  beforeEach(() => {
    component = new SuggestionsComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
