import { ExpansionPanelComponent } from './expansion-panel.component';

describe('ExpansionPanelComponent', () => {
  let component: ExpansionPanelComponent;

  beforeEach(() => {
    component = new ExpansionPanelComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
